general:
  artifacts:
    - "selenium/screenshots" # relative to the build directory
    - "./app" # relative to the user's home directory
    - "test.txt" # a single file, relative to the build directory